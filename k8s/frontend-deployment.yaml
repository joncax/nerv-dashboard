apiVersion: apps/v1
kind: Deployment
metadata:
  name: nerv-dashboard-frontend
  namespace: nerv-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nerv-dashboard-frontend
  template:
    metadata:
      labels:
        app: nerv-dashboard-frontend
    spec:
      containers:
        - name: frontend
          image: nerv-dashboard-frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nerv-dashboard-frontend
  namespace: nerv-dashboard
spec:
  type: NodePort
  selector:
    app: nerv-dashboard-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
